// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"igbinary_serialize", T(String), S(0), "var", T(Variant), NULL, NULL, S(0), NULL, S(16793600), "/**\n * ( excerpt from http://php.net/manual/en/function.igbinary-serialize.php\n * )\n *\n * Serialize an object into a string\n *\n * @var        mixed   The variable to serialize\n *\n * @return     string  The serialized object\n */", 
"igbinary_unserialize", T(Variant), S(0), "str", T(String), NULL, NULL, S(0), NULL, S(16793600), "/**\n * ( excerpt from\n * http://php.net/manual/en/function.igbinary-unserialize.php )\n *\n * Unserialize a string back to object\n *\n * @str        string  String representatin of the serialized object\n *                     (output of igbinary_serialize)\n *\n * @return     mixed   The restored object\n */", 

#elif EXT_TYPE == 1

#elif EXT_TYPE == 2

#elif EXT_TYPE == 3

#endif
