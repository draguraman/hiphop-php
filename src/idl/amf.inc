// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"amf_encode", T(String), S(0), "var", T(Variant), NULL, NULL, S(1), "flag", T(Variant), "N;", "null", S(1), "callback", T(Variant), "N;", "null", S(1), "output", T(Variant), "N;", "null", S(1), NULL, S(16793600), "/**\n * ( excerpt from http://php.net/manual/en/function.amf-encode.php )\n *\n * encode object into amf string\n *\n * @var        mixed   The variable to encode\n * @flag       mixed   The flag\n * @callback   mixed   call back\n * @output     mixed   output\n *\n * @return     string  Returns the string of encoded object.\n */", 
"amf_decode", T(Variant), S(0), "var", T(String), NULL, NULL, S(0), "flag", T(Variant), "N;", "null", S(1), "offset", T(Variant), "N;", "null", S(1), NULL, S(16793600), "/**\n * ( excerpt from http://php.net/manual/en/function.amf-decode.php )\n *\n * decode string to hiphop object.\n *\n * @var        string  String for encoded obj\n * @flag       mixed   The flag\n * @offset     mixed   offset\n *\n * @return     mixed   The stored variable or array of variables on\n *                     success; FALSE on failure\n */", 

#elif EXT_TYPE == 1

#elif EXT_TYPE == 2

#elif EXT_TYPE == 3

#endif
